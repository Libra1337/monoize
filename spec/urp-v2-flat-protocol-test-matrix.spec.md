# URP v2 Flat Protocol Test Matrix Specification

## 0. Status

- Version: `1.0.0`
- Product name: Monoize
- Internal protocol name: `URP v2`
- Scope: doc driven protocol contract matrix for flat URP v2 work on downstream `/v1/responses`, `/v1/chat/completions`, and `/v1/messages`, plus Monoize internal flat URP invariants that later tests must enforce.

## 1. Purpose and precedence

PTM-1. This file is the repository local source of truth for protocol contract test coverage required by the flat URP v2 redesign.

PTM-2. Each matrix row defines one behavior that later implementation or test tasks MUST verify.

PTM-3. Each matrix row MUST cite at least one authoritative source. Valid authorities are official external protocol documents and existing Monoize specs when the row is Monoize internal rather than protocol native.

PTM-4. Each matrix row MUST name one concrete target test location. A target location is either:

1. an existing test function, or
2. a planned test function or planned subsection theme in an existing test file.

PTM-5. `Existing` means the named test already exists in the repository at the time this matrix was written. `Planned` means the named test does not yet exist and MUST be added before the dependent flat URP v2 task is considered complete.

PTM-6. This file is a testing contract, not an implementation summary. If current implementation behavior disagrees with an authoritative source cited here, the source and referenced Monoize specs win.

## 2. Source register

| Source ID | Authority |
| --- | --- |
| `SRC-OAI-RESP-STREAM` | OpenAI, Streaming responses guide, <https://developers.openai.com/api/docs/guides/streaming-responses> |
| `SRC-OAI-RESP-EVENTS` | OpenAI, Responses streaming events reference, <https://developers.openai.com/api/reference/resources/responses/streaming-events/> |
| `SRC-OAI-RESP-ITEMS` | OpenAI, Responses input items reference, <https://developers.openai.com/api/reference/python/resources/responses/subresources/input_items/> |
| `SRC-OAI-RESP-CREATE` | OpenAI, Create a response, <https://developers.openai.com/api/reference/resources/responses/methods/create> |
| `SRC-OAI-COMPACTION` | OpenAI, Compaction, <https://developers.openai.com/api/docs/guides/compaction> |
| `SRC-OAI-PTC` | OpenAI, Programmatic tool calling, <https://developers.openai.com/api/docs/guides/tools-programmatic-tool-calling> |
| `SRC-OAI-TOOL-SEARCH` | OpenAI, Tool search, <https://developers.openai.com/api/docs/guides/tools-tool-search> |
| `SRC-OAI-RESP-ENCRYPTED` | OpenAI, Preserve reasoning without stored responses, <https://developers.openai.com/api/docs/guides/reasoning#preserve-reasoning-without-stored-responses> |
| `SRC-OAI-RAW-COT` | OpenAI, Handle gpt-oss raw chain of thought in the Responses API, <https://developers.openai.com/cookbook/articles/gpt-oss/handle-raw-cot#responses-api> |
| `SRC-OAI-CHAT-CREATE` | OpenAI, Create chat completion, <https://developers.openai.com/api/reference/resources/chat/subresources/completions/methods/create> |
| `SRC-OAI-CHAT-STREAM` | OpenAI, Chat Completions streaming events, <https://developers.openai.com/api/reference/resources/chat/subresources/completions/streaming-events> |
| `SRC-ANTH-STREAM` | Anthropic, Streaming Messages API guide, <https://platform.claude.com/docs/en/build-with-claude/streaming> |
| `SRC-ANTH-EVENTS` | Anthropic, Streaming event types, <https://platform.claude.com/docs/en/build-with-claude/streaming#event-types> |
| `SRC-ANTH-FULL` | Anthropic, Full HTTP stream response example, <https://platform.claude.com/docs/en/build-with-claude/streaming#full-http-stream-response> |
| `SRC-ANTH-MSG-CREATE` | Anthropic, Create a Message, <https://platform.claude.com/docs/en/api/messages/create> |
| `SRC-ANTH-EXTENDED-THINKING` | Anthropic, Extended thinking, <https://platform.claude.com/docs/en/build-with-claude/extended-thinking> and Adaptive thinking, <https://platform.claude.com/docs/en/build-with-claude/adaptive-thinking> |
| `SRC-ANTH-FILES` | Anthropic, Files API, <https://platform.claude.com/docs/en/build-with-claude/files> |
| `SRC-ANTH-STRUCTURED-OUTPUTS` | Anthropic, Structured outputs, <https://platform.claude.com/docs/en/build-with-claude/structured-outputs> |
| `SRC-ANTH-PTC` | Anthropic, Programmatic tool calling, <https://platform.claude.com/docs/en/agents-and-tools/tool-use/programmatic-tool-calling> |
| `SRC-ANTH-TOOL-SEARCH` | Anthropic, Tool search tool, <https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool> |
| `SRC-OR-OVERVIEW` | OpenRouter, API overview, <https://openrouter.ai/docs/api/reference/overview> |
| `SRC-OR-CHAT` | OpenRouter, Send chat completion request, <https://openrouter.ai/docs/api/api-reference/chat/send-chat-completion-request> |
| `SRC-OR-STREAM` | OpenRouter, Streaming reference, <https://openrouter.ai/docs/api/reference/streaming> |
| `SRC-OR-PARAMS` | OpenRouter, Parameters reference, <https://openrouter.ai/docs/api/reference/parameters> |
| `SRC-OR-REASONING` | OpenRouter, Reasoning tokens guide, <https://openrouter.ai/docs/guides/best-practices/reasoning-tokens> |
| `SRC-DS-CHAT` | DeepSeek, Create Chat Completion, <https://api-docs.deepseek.com/api/create-chat-completion> |
| `SRC-DS-THINKING` | DeepSeek, Thinking mode, <https://api-docs.deepseek.com/guides/thinking_mode> |
| `SRC-DS-TOOLS` | DeepSeek, Function calling, <https://api-docs.deepseek.com/guides/function_calling> |
| `SRC-URPV2` | `spec/urp-v2-flat-structure.spec.md` |
| `SRC-URPTF` | `spec/urp-transform-system.spec.md` |
| `SRC-PROXY` | `spec/unified_responses_proxy.spec.md` |

## 3. Matrix

### 3.0 Direct protocol route grid

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `GRID-REQ-1` | Every downstream request family in `{responses, chat_completion, messages}` MUST route to every upstream provider family in the same set in both non-stream and stream mode. The captured upstream body MUST use only the selected upstream family's request envelope, MUST contain the original user text semantically, and MUST preserve the requested stream boolean. This is an explicit 3 by 3 grid for each mode, not inferred transitively from separate adapter tests. | `SRC-OAI-RESP-CREATE`; `SRC-OAI-CHAT-CREATE`; `SRC-ANTH-MSG-CREATE`; `SRC-PROXY` `FP1` through `FP4` | Existing: `tests/api/adapters_nonstream.rs::direct_request_matrix_captures_all_openai_anthropic_routes_nonstream_and_stream`. |
| `GRID-RESP-1` | Every cell in `GRID-REQ-1` MUST return the selected downstream family's response envelope. Non-stream Responses output has a Responses object and `output[]`; Chat output has `choices[]`; Messages output has `type="message"` and `content[]`. Stream Responses uses named response events and `[DONE]`; Chat uses data-only chunks and `[DONE]`; Messages uses named Messages events and no `[DONE]`. This is an explicit 3 by 3 response grid for each mode. | `SRC-OAI-RESP-STREAM`; `SRC-OR-STREAM`; `SRC-ANTH-STREAM`; `SRC-PROXY` `FP6a` through `FP6g` | Existing: `tests/api/adapters_nonstream.rs::direct_request_matrix_captures_all_openai_anthropic_routes_nonstream_and_stream`. |

### 3.1 Responses non stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `RSP-NS-1` | For downstream `/v1/responses` in non stream mode, top level unknown request and response fields MUST survive same family forwarding. Nested node local passthrough and envelope local passthrough MUST remain eligible for same family preservation. | `SRC-URPV2` `URPV2-6` through `URPV2-7`, `XTRA-1` through `XTRA-3`; `SRC-PROXY` `F1` | Existing: `tests/api/adapters_nonstream/messages_basic.rs::responses_nonstream_same_family_node_and_envelope_passthrough`. |
| `RSP-NS-2` | A Responses family tool result input MUST decode into one top level `ToolResult` node, preserve multipart tool result content order, and re encode without collapsing images or files into plain text. | `SRC-OAI-RESP-ITEMS`; `SRC-URPV2` `TR-1` through `TR-9`; `SRC-PROXY` `TCI1`, `TRC1` through `TRC2`, `ENC1` through `ENC4` | Existing: `tests/api/adapters_nonstream.rs::responses_tool_result_multipart_roundtrip_via_responses_upstream`. |
| `RSP-NS-3` | Cross protocol non stream tool calling into a chat upstream MUST preserve one reasoning unit plus one function call item per tool call, and a follow up Responses tool result request MUST reach the model as tool output rather than ordinary assistant text. | `SRC-URPV2` `TR-1` through `TR-9`; `SRC-PROXY` `TCI1` through `TCI3`, `ENC1` through `ENC4` | Existing: `tests/api/adapters_nonstream.rs::responses_tool_call_flow_nonstream_via_chat_upstream_parallel`. |
| `RSP-NS-4` | Downstream shape: `/v1/responses` request with a `tools[]` function object that includes official function fields plus extra function level fields such as `strict` and one unknown extension. Upstream target: `type=responses`. Expected encoded and decoded tool shape: one flat Responses function tool with `type="function"`, `name`, `description`, `parameters`, `strict`, and the extension at the tool object level, not inside a nested `function` object. Failure condition: the encoder drops `strict`, nests the function shape, or moves the extension into top level request `extra_body`. | `SRC-OAI-RESP-CREATE`; `SRC-URPV2` `URPV2-6` through `URPV2-7`, `XTRA-1` through `XTRA-3`; `SRC-PROXY` `T0`, `PR4`, `XF6d` | Existing local: `src/urp/decode/mod.rs::tests::parse_tool_definition_accepts_function_and_custom_shapes`, `src/urp/encode/openai_responses/media.inc.rs::tests::responses_function_tool_preserves_extras`; API: `tests/api/adapters_nonstream/images_and_chat.rs::responses_tool_definition_function_extras_preserved_for_responses_upstream`. |
| `RSP-NS-5` | Downstream shape: `/v1/responses` request with one custom tool and one native built-in tool selected from `file_search`, `code_interpreter`, `web_search`, `mcp`, `namespace`, `tool_search`, or `programmatic_tool_calling`. Upstream target: `type=responses`. Expected encoded and decoded tool shape: the custom tool remains a flat Responses custom tool, and each built-in remains a flat non-function tool with its native `type` and native fields. Failure condition: any custom tool or built-in is converted to `type="function"`, loses its native fields, or triggers local tool execution. | `SRC-OAI-RESP-CREATE`; `SRC-URPV2` `XTRA-1` through `XTRA-3`; `SRC-PROXY` `T1a`, `PR4`, `TCI2` | Existing local: `src/urp/decode/mod.rs::tests::parse_tool_definition_accepts_function_and_custom_shapes`, `src/urp/encode/openai_responses/media.inc.rs::tests::responses_custom_tool_preserves_flat_fields`, `src/urp/encode/openai_responses/media.inc.rs::tests::responses_builtins_remain_native_and_preserve_config`; API: `tests/api/adapters_nonstream/tools_envelope.rs::responses_custom_and_builtin_tools_are_forwarded_as_native_descriptors`. |
| `RSP-NS-6` | `POST /v1/responses/compact` MUST route only to a Responses provider, call upstream `/v1/responses/compact`, preserve the ordered native compact input, and return `object="response.compaction"`, opaque compaction output, and usage unchanged. A request with `stream=true` MUST fail before upstream dispatch. | `SRC-OAI-COMPACTION`; `SRC-PROXY` `CMP1` through `CMP5` | Existing API: `tests/api/adapters_nonstream/native_responses.rs::responses_compact_is_native_same_protocol_passthrough`, `tests/api/adapters_nonstream/native_responses.rs::responses_compact_rejects_streaming_before_upstream_dispatch`. |
| `RSP-NS-7` | A same-Responses programmatic tool call lifecycle MUST preserve the native PTC descriptor, deferred function metadata, `program`, correlated `function_call.caller`, client `function_call_output.caller`, and `program_output`. A continuation containing only the client result plus `previous_response_id` MUST not lose that result to stateless orphan filtering. | `SRC-OAI-PTC`; `SRC-PROXY` `S3`, `TCI5`, `TCI6` | Existing API: `tests/api/adapters_nonstream/native_responses.rs::responses_programmatic_tool_calling_round_trips_and_stateful_result_survives`. |
| `RSP-NS-8` | A same-Responses tool-search lifecycle MUST preserve deferred tool declarations, `namespace`, `tool_search`, `tool_search_call`, `tool_search_output`, and `additional_tools` as native Responses shapes in request and response order. | `SRC-OAI-TOOL-SEARCH`; `SRC-PROXY` `TCI7` | Existing API: `tests/api/adapters_nonstream/native_responses.rs::responses_tool_search_lifecycle_round_trips_same_family`. |
| `RSP-NS-9` | A normal `/v1/responses` request MUST preserve same-family `context_management` configuration and replayed opaque compaction input. A native compaction output item MUST remain top-level, opaque, and ordered in the downstream Responses output. | `SRC-OAI-COMPACTION`; `SRC-PROXY` `CMP6`, `PI3` through `PI6` | Existing API: `tests/api/adapters_nonstream/native_responses.rs::responses_context_management_and_compaction_item_round_trip_same_family`. |

### 3.2 Responses stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `RSP-ST-1` | A downstream `/v1/responses` stream MUST use named SSE events, MUST send payload JSON at top level without a synthetic `data` wrapper, and MUST end with exactly one plain `data: [DONE]` sentinel. Every emitted Responses event payload MUST include all top-level fields listed for that event in `openai-responses-streaming-events.md`; for `response.created`, `response.in_progress`, and `response.completed`, the nested `response` object MUST include `user`, with value `null` when no user is known. | `SRC-OAI-RESP-STREAM`; `SRC-OAI-RESP-EVENTS`; `SRC-PROXY` `FP6a`, `FP6a4`, `PR3` | Existing: `tests/api/streaming_responses.rs::responses_streaming_emits_sse_events`, `tests/api/streaming_responses.rs::responses_streaming_emits_single_plain_done_sentinel`. |
| `RSP-ST-2` | Responses item and part lifecycle boundaries MUST remain externally visible. The stream MUST preserve output item order, stable `output_index`, stable `item_id`, stable `content_index`, and protocol correct `response.output_item.*`, `response.content_part.*`, and text delta events. One RawCoT reasoning item MUST use one canonical reasoning node across its output-item, `reasoning_text`, and `content_part` lifecycle. | `SRC-OAI-RESP-STREAM`; `SRC-OAI-RESP-EVENTS`; `SRC-OAI-RESP-ITEMS`; `SRC-OAI-RAW-COT`; `SRC-URPV2` `STR-5` through `STR-8`; `SRC-PROXY` `PR2a`, `FP6g` | Existing: `tests/api/streaming_responses.rs::responses_streaming_reencodes_greedy_merged_items_with_canonical_sse_boundaries`, `tests/api/streaming_responses.rs::responses_streaming_uses_top_level_payload_fields_and_delta_ids`, `tests/api/streaming_responses/tool_lifecycle.rs::responses_streaming_raw_cot_uses_one_reasoning_node_and_full_content_lifecycle`. |
| `RSP-ST-3` | Reasoning summary text and full reasoning text are distinct observable surfaces. A Responses stream MUST emit separate summary and reasoning events, MUST preserve explicit reasoning source when upstream provides one, MUST omit `source` when upstream omits it, and MUST reconcile delta accumulation with later full summary snapshots. If `response.output_item.added` and `response.output_item.done` carry different complete encrypted snapshots, the downstream stream MUST preserve both snapshots in their matching lifecycle events. The terminal accumulator MUST use the done-event snapshot as one atomic value and MUST NOT merge it with the added-event snapshot. A non-empty terminal response-object summary remains authoritative without weakening conflict checks for reasoning text or terminal encrypted content. | `SRC-OAI-RESP-EVENTS`; `SRC-URPV2` `RSN-1` through `RSN-8`, `SACC-3` through `SACC-8`; `SRC-PROXY` `DER10` through `DER13`, `PR4c.5b`, `PR6c` | Existing: `tests/api/streaming_responses.rs::responses_streaming_distinguishes_reasoning_summary_and_content`, `tests/api/streaming_responses.rs::responses_streaming_preserves_explicit_reasoning_source_from_chat_upstream`, `tests/api/streaming_responses.rs::responses_streaming_omits_reasoning_source_when_chat_upstream_does_not_send_one`, `tests/api/streaming_responses/completed_state.rs::responses_streaming_terminal_summary_snapshot_overrides_streamed_summary`, `tests/api/streaming_responses.rs::responses_streaming_preserves_distinct_encrypted_reasoning_snapshots`. |
| `RSP-ST-4` | When upstream terminal state is available only from completed item payloads, Monoize MUST reconstruct downstream text deltas and message phase from terminal item state without inventing extra message items. | `SRC-OAI-RESP-EVENTS`; `SRC-URPV2` `ORD-5`, `STR-7` through `STR-9`; `SRC-PROXY` `PR2a`, `FP6g` | Existing: `tests/api/streaming_responses.rs::responses_streaming_reconstructs_text_from_output_item_done`, `tests/api/streaming_responses.rs::responses_streaming_reconstructs_phase_from_output_item_done`. |
| `RSP-ST-5` | Completed item replay MUST NOT duplicate output item lifecycles. If `ResponseDone.output` already contains the terminal node set, downstream Responses SSE MUST expose each completed item exactly once. | `SRC-URPV2` `STR-8` through `STR-11`; `SRC-URPTF` `STR-4` through `STR-5`; `SRC-PROXY` `FP6g` | Existing: `tests/api/streaming_responses.rs::responses_streaming_from_responses_upstream_does_not_duplicate_completed_items`, `tests/api/streaming_responses.rs::responses_streaming_response_done_output_is_authoritative_terminal_state`. |
| `RSP-ST-6` | Same-Responses streaming MUST preserve the ordered output-item lifecycle and terminal output for native `program`, programmatic `function_call.caller`, `program_output`, `tool_search_call`, `tool_search_output`, and `compaction` items without converting their types or duplicating them. | `SRC-OAI-PTC`; `SRC-OAI-TOOL-SEARCH`; `SRC-OAI-COMPACTION`; `SRC-PROXY` `CMP6`, `TCI6`, `TCI7` | Existing API: `tests/api/streaming_responses/tool_lifecycle.rs::responses_streaming_preserves_native_ptc_tool_search_and_compaction_items`. |

### 3.3 Chat Completions non stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `CHT-NS-1` | For downstream `/v1/chat/completions` in non stream mode, basic assistant content and top level unknown fields MUST survive the adapter boundary. | `SRC-URPV2` `URPV2-6` through `URPV2-7`, `XTRA-1`; `SRC-PROXY` `F1`, `ENC6` through `ENC8` | Existing: `tests/api/adapters_nonstream.rs::chat_completions_adapter_nonstream`. |
| `CHT-NS-2` | Chat reasoning controls and reasoning output MUST follow OpenRouter compatible semantics. Plaintext reasoning MAY surface through the simple `reasoning` alias, but structured reasoning MUST surface in `reasoning_details`, and encrypted reasoning MUST remain a distinct detail item rather than a synthetic signature field. | `SRC-OR-CHAT`; `SRC-OR-PARAMS`; `SRC-OR-REASONING`; `SRC-PROXY` `RC1` through `RC4a`, `ENC8`, `DER10` through `DER13` | Existing: `tests/api/adapters_nonstream.rs::chat_reasoning_effort_maps_to_responses_upstream_reasoning`, `tests/api/adapters_nonstream.rs::chat_reasoning_effort_maps_to_messages_upstream_thinking`, `tests/api/adapters_nonstream.rs::chat_reasoning_effort_maps_to_chat_upstream_encrypted_reasoning`. |
| `CHT-NS-3` | OpenRouter chat request extension fields, including `models`, `route`, `provider`, `plugins`, `user`, and debug request fields, MUST survive flat URP decode and target family encode unless a typed field intentionally consumes that exact key. | `SRC-OR-OVERVIEW`; `SRC-OR-CHAT`; `SRC-OR-PARAMS`; `SRC-URPV2` `URPV2-6` through `URPV2-7`, `XTRA-1` through `XTRA-6`; `SRC-PROXY` `F1` | Existing: `tests/api/adapters_nonstream.rs::chat_nonstream_openrouter_request_extensions_passthrough`. |
| `CHT-NS-4` | If a downstream chat request continues a conversation with prior `reasoning_details`, Monoize MUST preserve returned reasoning detail order, MUST NOT derive scalar reasoning aliases for same-Chat replay, and MUST add a summary-compatible input when the selected upstream family requires one. | `SRC-OR-REASONING`; `SRC-OR-CHAT`; `SRC-PROXY` `RC2` through `RC4e`, `DER10` | Existing: `tests/api/adapters_nonstream.rs::chat_to_responses_upstream_reasoning_inputs_always_include_summary`, `tests/api/adapters_nonstream.rs::chat_nonstream_reasoning_details_replay_preserves_order`. |
| `CHT-NS-5` | Cross protocol non stream tool calling through chat responses MUST preserve assistant `tool_calls` shape on the way out and accept downstream tool role results on the way back in. | `SRC-OR-CHAT`; `SRC-URPV2` `TCL-1` through `TCL-2`, `TR-1` through `TR-9`; `SRC-PROXY` `TCI1` through `TCI3`, `ENC6` | Existing: `tests/api/adapters_nonstream.rs::chat_tool_call_flow_nonstream_via_responses_upstream_parallel`. |
| `CHT-NS-6` | Downstream shape: `/v1/chat/completions` request with a Chat Completions custom tool using nested shape `{"type":"custom","custom":{...}}`. Upstream target: `type=chat_completion`. Expected encoded and decoded tool shape: the custom tool keeps the nested `custom` object and does not become a flat Responses custom tool while the target family remains chat. Failure condition: the custom payload is flattened, rewritten as a function tool, or stored only as unknown top level request passthrough. | `SRC-OAI-CHAT-CREATE`; `SRC-URPV2` `XTRA-1` through `XTRA-3`; `SRC-PROXY` `T1`, `XF6d` | Existing local: `src/urp/decode/mod.rs::tests::parse_tool_definition_accepts_function_and_custom_shapes`, `src/urp/encode/openai_chat.rs::tests::openai_chat_custom_tool_round_trips`; API: `tests/api/adapters_nonstream/controls_tools_matrix.rs::openai_custom_tool_definitions_use_target_family_shape`. |
| `CHT-NS-7` | Chat `file` content parts using nested `file_id` or `file_data` and Chat `input_audio` parts MUST decode into typed URP media. Same-Chat encoding MUST restore the official nested shapes. OpenAI-origin file IDs MAY map between Chat and Responses, but MUST NOT map to Anthropic Files IDs. Unsupported URL files and audio targets MUST be omitted rather than converted to synthetic text. | `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-URPV2` `URPV2-13a` through `URPV2-13c`; `SRC-PROXY` `PC2.5` through `PC2.6` | Existing local: `src/urp/decode/openai_chat.rs::tests::chat_file_and_audio_parts_decode_to_typed_media`; API: `tests/api/adapters_nonstream/images_and_chat.rs::chat_file_and_audio_inputs_round_trip_and_map_only_to_supported_targets`. |
| `CHT-NS-8` | DeepSeek Chat requests MUST use current thinking-mode controls and `max_tokens`; assistant tool-loop history MUST replay the prior non-empty `reasoning_content` required by DeepSeek. Disabled thinking MUST omit `reasoning_effort`; enabled levels MUST use the documented DeepSeek effort mapping without introducing Responses-only fields. | `SRC-DS-CHAT`; `SRC-DS-THINKING`; `SRC-DS-TOOLS`; `SRC-PROXY` `DC5a`, `DC5c` | Existing local: `src/urp/encode/openai_chat.rs::tests::deepseek_request_uses_current_thinking_and_token_controls`, `src/urp/encode/openai_chat.rs::tests::deepseek_tool_loop_replays_reasoning_content_without_openrouter_aliases`. |
| `CHT-NS-9` | OpenRouter embedded error objects MUST terminate as failure. String or numeric codes and documented metadata fallbacks MUST survive in the downstream error object; no error response may become a successful `finish_reason="stop"` completion. | `SRC-OR-OVERVIEW`; `SRC-OR-STREAM`; `SRC-PROXY` `DC5b` | Existing: `tests/api/adapters_nonstream.rs::chat_nonstream_openrouter_errors_do_not_become_successful_completions`; local: `src/upstream.rs::tests::openrouter_error_info_accepts_numeric_code_and_metadata_fallbacks`. |
| `CHT-NS-10` | A downstream Chat request MAY omit `n` or set `n=1`. Every other `n` shape MUST fail before dispatch because URP carries one assistant candidate and MUST NOT silently discard later choices. | `SRC-OAI-CHAT-CREATE`; `SRC-PROXY` `DC1a` | Existing: `tests/api/adapters_nonstream.rs::chat_multiple_choices_are_rejected_before_upstream_dispatch`; local: `src/urp/decode/openai_chat.rs::tests::chat_request_rejects_multiple_choices`. |
| `CHT-NS-11` | A Chat assistant response containing a message-level `audio` object MUST preserve that exact object on a same-Chat decode and encode, including an audio-only response whose `content` is `null`. A Chat content block whose explicit `type` is not a recognized semantic type MUST remain an opaque same-Chat ProviderItem even when it contains a string `text` member. Neither surface may be textified during same-Chat replay. | `SRC-OAI-CHAT-CREATE`; `SRC-URPV2` `XTRA-4` through `XTRA-7`; `SRC-PROXY` `PC2.3`, `PC2.8` | Existing local: `src/urp/decode/openai_chat.rs::tests::chat_audio_only_response_replays_message_audio_and_null_content`, `src/urp/decode/openai_chat.rs::tests::unknown_typed_text_block_remains_provider_item_and_round_trips`. |
| `CHT-NS-12` | Deprecated Chat assistant `function_call` and `role="function"` result messages MUST normalize into one marked function call and result lifecycle. Same-Chat encoding MUST restore `function_call` and `role="function"` wire shapes, preserve the function name and arguments, and MUST NOT expose Monoize provenance markers. | `SRC-OAI-CHAT-CREATE`; `SRC-PROXY` `PC2.9` | Existing local: `src/urp/decode/openai_chat.rs::tests::deprecated_function_call_and_function_result_round_trip_as_legacy_messages`. |

### 3.4 Chat Completions stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `CHT-ST-1` | A downstream `/v1/chat/completions` stream MUST be data only SSE, MUST emit exactly one plain `data: [DONE]`, and MUST NOT emit named SSE `event:` lines. | `SRC-OR-STREAM`; `SRC-PROXY` `FP6a`, `FP6c` through `FP6g` | Existing: `tests/api/streaming_chat.rs::chat_streaming_emits_single_plain_done_and_no_named_events`. |
| `CHT-ST-2` | The terminal finish chunk MUST be separate from content and tool call deltas. A chunk MUST NOT co pack `delta.content` and `delta.tool_calls`, and the terminal finish chunk MUST carry an empty delta with one finish reason only. | `SRC-OR-STREAM`; `SRC-OR-CHAT`; `SRC-PROXY` `ENC6`, `FP6g` | Existing: `tests/api/streaming_chat.rs::chat_streaming_content_then_tool_call_keeps_finish_reason_terminal`. |
| `CHT-ST-3` | If downstream tool calls were emitted, terminal finish normalization MUST be `tool_calls`, including completed fallback cases, content array tool cases, and header only tool call cases. Tool call argument fragments MUST reassemble by tool call index. | `SRC-OR-STREAM`; `SRC-PROXY` `ENC6`, `FP6g` | Existing: `tests/api/streaming_chat.rs::chat_streaming_maps_tool_calls_from_responses_completed_fallback`, `tests/api/streaming_chat.rs::chat_streaming_keeps_chat_upstream_terminal_tool_calls_reason`, `tests/api/streaming_chat.rs::chat_streaming_normalizes_chat_upstream_stop_to_tool_calls_when_tools_emitted`, `tests/api/streaming_chat.rs::chat_streaming_parallel_tool_calls_from_chat_upstream_reassembles_arguments`, `tests/api/streaming_chat.rs::chat_streaming_content_array_tool_call_keeps_tool_loop_alive`, `tests/api/streaming_chat.rs::chat_streaming_content_array_tool_use_keeps_tool_loop_alive`, `tests/api/streaming_chat.rs::chat_streaming_header_only_tool_call_still_finishes_as_tool_calls`. |
| `CHT-ST-4` | OpenRouter reasoning stream semantics are normative for downstream chat. Summary details, plaintext reasoning details, and encrypted reasoning details MUST remain distinct ordered `reasoning_details` entries. Each encrypted detail is one complete opaque entry: envelope wrapping MUST update both its typed encrypted value and preserved raw `data` value to the same single envelope, and MUST NOT merge it with another detail entry. Legacy scalar encrypted fragments MUST produce one complete envelope per canonical node, and a different terminal full snapshot MUST replace the scalar accumulator atomically. Structured reasoning MUST NOT be moved into `delta.reasoning`, plaintext reasoning detail rows MUST NOT gain a synthetic `signature` field, and plaintext `Reasoning.content` MUST NOT become a summary unless an explicit response transform already performed that conversion. | `SRC-OR-STREAM`; `SRC-OR-REASONING`; `SRC-PROXY` `ENC8`, `ENC8a`, `RCD-9`, `DER10` through `DER13`, `PR4c.5c` | Existing: `tests/api/streaming_chat.rs::chat_streaming_preserves_summary_vs_reasoning_in_openrouter_extension`, `tests/api/streaming_chat.rs::chat_streaming_preserves_encrypted_reasoning_from_chat_upstream`, `tests/api/streaming_chat.rs::chat_streaming_wraps_legacy_opaque_fragments_once_and_uses_terminal_snapshot_atomically`, `tests/api/streaming_chat.rs::chat_streaming_reasoning_details_preserve_raw_entries_and_arrival_order`, `tests/api/streaming_chat.rs::chat_streaming_maps_tool_calls_and_reasoning_from_responses_upstream`. |
| `CHT-ST-5` | If a response transform converts plaintext reasoning into summary text, downstream chat streaming MUST emit only OpenRouter style summary detail rows for that plaintext value and MUST still preserve encrypted reasoning rows unchanged. | `SRC-OR-STREAM`; `SRC-OR-REASONING`; `SRC-URPTF` `PRTS-3` through `PRTS-9`; `SRC-PROXY` `RCD-9` | Existing: `tests/api/streaming_chat.rs::chat_streaming_plaintext_reasoning_to_summary_rewrites_reasoning_events`, `tests/api/streaming_chat.rs::chat_streaming_plaintext_reasoning_to_summary_preserves_encrypted_reasoning`. |
| `CHT-ST-6` | OpenRouter final usage chunk semantics are normative for downstream chat. When usage is available, the stream MUST emit one empty-delta finish chunk without non-null usage, then exactly one `choices=[]` usage chunk, then `[DONE]`. The usage and finish chunks MUST share the same `id`, `object`, `created`, and `model` envelope values. | `SRC-OR-STREAM`; `SRC-OR-OVERVIEW`; `SRC-PROXY` `DC7`, `DC9` | Existing: `tests/api/streaming_chat.rs::chat_streaming_from_responses_includes_terminal_usage`, `tests/api/streaming_chat.rs::chat_streaming_content_only_from_chat_upstream_has_terminal_chunk_and_usage`, `tests/api/streaming_chat.rs::chat_streaming_openrouter_final_usage_chunk_shape`. |
| `CHT-ST-7` | A same-family Chat stream MUST reconstruct missing text, reasoning, and tool-argument suffixes from a terminal `choices[0].message` snapshot without duplicating streamed prefixes. Non-internal unknown delta and terminal-message fields MUST survive exactly once before the single terminal finish chunk. Internal `_monoize_` fields MUST NOT appear on the wire. | `SRC-OR-STREAM`; `SRC-PROXY` `PC7c` through `PC7f`, `ENC8`, `XTRA-10` | Existing: `tests/api/streaming_chat.rs::chat_streaming_terminal_message_snapshot_emits_only_missing_suffixes_and_native_extras`. |
| `CHT-ST-8` | A choice-level DeepSeek `logprobs` object on a non-terminal token frame MUST remain choice-level on a non-terminal downstream frame. It MUST NOT move below `delta`, be delayed to the terminal frame, or disappear. | `SRC-DS-CHAT`; `SRC-PROXY` `DC6` | Existing: `tests/api/streaming_chat.rs::chat_streaming_preserves_choice_logprobs_on_nonterminal_frame`; local: `src/urp/stream_encode/openai_chat.rs::tests::chat_stream_emits_choice_level_logprobs_on_token_frame`. |
| `CHT-ST-9` | Deprecated Chat `delta.function_call` fragments MUST normalize into one marked flat function call node and preserve fragment order. Same-Chat streaming encode MUST emit `delta.function_call`, MUST NOT emit `delta.tool_calls` for that node, MUST finish with `finish_reason="function_call"`, and MUST emit exactly one `[DONE]`. | `SRC-OAI-CHAT-STREAM`; `SRC-PROXY` `PC2.9` | Existing local: `src/urp/stream_decode/openai_chat.rs::tests::legacy_function_call_deltas_become_one_marked_tool_call_node`, `src/urp/stream_encode/openai_chat.rs::tests::legacy_function_call_stream_replays_deprecated_shape_and_finish_reason`. |
| `CHT-ST-10` | A top-level or choice-local OpenRouter error chunk MUST terminate the stream as failure and emit one `[DONE]`. A missing direct `error.code` or `error.type` MUST be materialized from `metadata.provider_code` or `metadata.error_type`. Same-Chat raw replay MUST preserve other native fields, direct native error members MUST win fallback values, and incoming `_monoize_` owner-layer members MUST NOT be emitted. | `SRC-OR-STREAM`; `SRC-URPV2` `XTRA-10`; `SRC-PROXY` `DC5b` | Existing API: `tests/api/streaming_chat.rs::chat_streaming_openrouter_error_chunks_are_terminal_and_preserved`; local: `src/urp/stream_encode/openai_chat.rs::tests::chat_raw_error_replay_materializes_metadata_fallbacks_and_filters_reserved_owners`. |

### 3.5 Messages non stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `MSG-NS-1` | For downstream `/v1/messages` in non stream mode, assistant text content and top level unknown fields MUST survive the adapter boundary. | `SRC-URPV2` `URPV2-6` through `URPV2-7`, `XTRA-1`; `SRC-PROXY` `F1`, `ENC9` through `ENC10` | Existing: `tests/api/adapters_nonstream.rs::messages_adapter_nonstream`. |
| `MSG-NS-2` | Anthropic style thinking output and tool use output MUST reconstruct from flat URP nodes without collapsing `thinking`, `tool_use`, or `tool_result` into plain text blocks. | `SRC-URPV2` `RSN-1` through `RSN-8`, `TR-1` through `TR-9`; `SRC-PROXY` `ENC9` through `ENC10` | Existing: `tests/api/adapters_nonstream.rs::messages_thinking_maps_to_chat_upstream_reasoning_effort`, `tests/api/adapters_nonstream.rs::messages_tool_call_flow_nonstream_via_responses_upstream_parallel`, `tests/api/adapters_nonstream.rs::messages_tool_result_multipart_roundtrip_via_messages_upstream`. |
| `MSG-NS-3` | Downstream `/v1/messages` requests MUST still send the required Anthropic version header when the selected upstream family is `messages`. | `SRC-PROXY` adapter rules for provider type `messages`; Anthropic HTTP contract as implemented by Monoize provider adapter | Existing: `tests/api/adapters_nonstream.rs::messages_upstream_sends_anthropic_version_header`. |
| `MSG-NS-4` | Downstream shape: `/v1/messages` request with a user-defined Anthropic tool containing `name`, `description`, `input_schema`, `cache_control`, `strict`, and `defer_loading`. Upstream target: `type=messages`. Expected encoded and decoded tool shape: one Anthropic tool object preserves those fields as tool object fields, with `input_schema` kept as the schema field and no synthetic Responses `parameters` field in the upstream Messages body. Failure condition: `cache_control`, `strict`, or `defer_loading` is dropped, moved to top level request `extra_body`, or emitted inside a tool result content block. | `SRC-ANTH-MSG-CREATE`; `SRC-URPV2` `XTRA-1` through `XTRA-3`; `SRC-PROXY` `T1`, `PM1`, `XF6d` | Existing local: `src/urp/encode/anthropic/tools.inc.rs::tests::anthropic_function_tool_preserves_extras_and_strict`, `src/urp/encode/anthropic/tools.inc.rs::tests::anthropic_tool_extra_layering_is_stable`; API: `tests/api/adapters_nonstream/messages_basic.rs::messages_tool_definition_metadata_preserved_same_family`, `tests/api/adapters_nonstream/messages_basic.rs::messages_tool_definition_metadata_maps_to_responses_upstream`. |
| `MSG-NS-5` | Downstream shape: `/v1/messages` request with Anthropic `tool_choice` variants `auto`, `any`, `none`, and named `tool`, including `disable_parallel_tool_use` on the supported object variants. Upstream targets: `type=messages` and OpenAI-compatible `type=chat_completion` or `type=responses`. Expected shape: Messages upstream re-emits `any` as `{type:"any"}` and named tool as `{type:"tool",name:<N>}` with disable flag preserved; OpenAI-compatible upstream emits `parallel_tool_calls` at request top level and never under `tools[]`. Failure condition: disable flag is moved into a tool descriptor, `{type:"any"}` fails to round-trip through canonical required, named tool loses its name, or `parallel_tool_calls` is nested below a tool object. | `SRC-ANTH-MSG-CREATE`; `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-PROXY` `PM7`, `XF6d` | Existing: `tests/api/adapters_nonstream/native_responses.rs::messages_tool_choice_any_roundtrips_for_messages_upstream`, `tests/api/adapters_nonstream/native_responses.rs::messages_tool_choice_none_roundtrips_for_messages_upstream`, `tests/api/adapters_nonstream/native_responses.rs::messages_tool_choice_tool_disable_parallel_roundtrips_for_messages_upstream`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::messages_tool_choice_variants_map_to_responses_upstream`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::messages_tool_choice_variants_map_to_chat_upstream`. |
| `MSG-NS-6` | Downstream shape: `/v1/messages` request with explicit `max_tokens`. Upstream target: `type=messages`. Expected shape: the decoded URP request stores the value as `max_output_tokens`, and the upstream Messages request body sends the same integer under `max_tokens` unchanged. Failure condition: the value is omitted, defaulted, renamed to a non-Messages field in the upstream body, or numerically changed. | `SRC-ANTH-MSG-CREATE`; `SRC-PROXY` `ENC9a` | Existing: `tests/api/adapters_nonstream.rs::messages_upstream_forwards_explicit_max_tokens_unchanged`. |
| `MSG-NS-7` | A same-family Messages request MUST preserve explicit `thinking` and `output_config` objects independently and byte-equivalently after JSON decode and encode. This includes disabled thinking, manual budget, adaptive display and every `output_config.format` field. A Messages `thinking` response or assistant-history block with `thinking=""` and a non-empty signature MUST remain an omitted-thinking block instead of being dropped or converted to `redacted_thinking`. Generated controls use adaptive thinking for Opus and Sonnet 4.6 or later and manual budgets only for bounded older families. | `SRC-ANTH-MSG-CREATE`; `SRC-PROXY` `RC4`, `RC4b` through `RC4d`, `PM5`, `PM5a`, `DM5.1` through `DM5.3` | Existing: `tests/api/adapters_nonstream.rs::messages_request_preserves_explicit_thinking_and_output_config`, `tests/api/adapters_nonstream.rs::messages_request_roundtrips_omitted_thinking_block`; local: `src/urp/decode/anthropic.rs::tests::messages_reasoning_controls_preserve_exact_objects`, `src/urp/encode/anthropic.rs::tests::explicit_messages_reasoning_controls_override_generation`, `src/urp/encode/anthropic.rs::tests::omitted_thinking_round_trips_as_empty_thinking_with_signature`, `src/urp/encode/anthropic.rs::tests::adaptive_model_detection_defaults_new_claude_families_to_adaptive`. |
| `MSG-NS-8` | A non-streaming same-family Messages response MUST re-emit the exact upstream `stop_reason`, including `pause_turn`, `refusal`, and `model_context_window_exceeded`. | `SRC-ANTH-MSG-CREATE`; `SRC-PROXY` `PM6.2` | Existing: `tests/api/adapters_nonstream.rs::messages_nonstream_preserves_exact_stop_reason`; local: `src/urp/decode/anthropic.rs::tests::omitted_thinking_round_trips_as_empty_thinking_with_signature`. |
| `MSG-NS-9` | Messages image and document blocks with `source = {type:"file",file_id:<id>}`, including blocks nested in `tool_result.content[]`, MUST decode to typed file-id sources with Messages provenance and re-encode without changing the identifier on a same-Messages hop. A Messages upstream attempt that contains such a block or a `container_upload` file reference MUST carry `anthropic-beta: files-api-2025-04-14`. OpenAI-origin file identifiers MUST NOT be translated into Anthropic file sources. | `SRC-ANTH-FILES`; `SRC-URPV2` `URPV2-13a` through `URPV2-13c`; `SRC-PROXY` `PM2b`, `PM2c.1` through `PM2c.2`, `PM8d` | Existing: `src/urp/decode/anthropic.rs::tests::messages_file_id_sources_round_trip`, `src/urp/decode/anthropic.rs::tests::messages_tool_result_file_ids_keep_files_api_provenance`, `tests/api/adapters_nonstream.rs::messages_file_id_sources_forward_with_files_beta_header`, `tests/api/adapters_nonstream.rs::provider_scoped_file_ids_are_not_cross_family_translated`, `tests/api/adapters_nonstream/messages_native.rs::messages_tool_result_file_ids_round_trip_with_files_beta_header`. |
| `MSG-NS-10` | An adjacent Chat `reasoning.text` detail followed by a Chat `reasoning.encrypted` detail MUST become one Messages `thinking` block in both downstream response encoding and upstream assistant-history request encoding. The plaintext MUST be the block's `thinking`; the encrypted detail MUST be that same block's `signature`, with direction-specific sigil handling. | `SRC-OR-REASONING`; `SRC-ANTH-MSG-CREATE`; `SRC-PROXY` `DM5.1` through `DM5.3`, `DM5a.1` | Existing: `tests/api/adapters_nonstream.rs::messages_thinking_maps_to_chat_upstream_reasoning_effort`, `tests/api/adapters_nonstream.rs::chat_reasoning_detail_pair_merges_for_messages_upstream_replay`. |
| `MSG-NS-11` | Messages `output_config.format.type=json_schema` MUST decode into typed `response_format`, preserve the complete explicit `output_config` for same-family replay, and map to Chat and Responses JSON-schema controls with deterministic name `response`. Chat or Responses JSON-schema controls MUST map to Messages `output_config.format` with only the Messages `type` and `schema` semantic fields; OpenAI-only `name`, `description`, and `strict` MUST NOT leak. Typed format state MUST win collisions while unknown top-level `output_config` siblings survive. | `SRC-ANTH-STRUCTURED-OUTPUTS`; `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-PROXY` `RC4b`, `RC4b.1`, `PM4.4`, `PM4.4.1` | Existing: `src/urp/decode/anthropic.rs::tests::messages_structured_output_decodes_to_canonical_format`, `src/urp/encode/anthropic/tools.inc.rs::tests::messages_structured_output_uses_output_config_format`, `tests/api/adapters_nonstream/messages_reasoning.rs::messages_structured_output_round_trips_and_maps_to_openai_families`, `tests/api/adapters_nonstream/messages_reasoning.rs::openai_json_schemas_map_to_messages_output_config_format`. |
| `MSG-NS-12` | Chat scalar or array `stop` MUST map to Messages `stop_sequences`; Messages `stop_sequences` MUST map to Chat array `stop`; Responses MUST omit stop controls. Chat top-level and Responses `text` verbosity MUST map bidirectionally while Messages omits it. Chat/Responses `user` and Messages `metadata.user_id` MUST map bidirectionally. Same-Messages replay MUST preserve unknown metadata siblings, and typed user identity MUST win a metadata collision. | `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-ANTH-MSG-CREATE`; `SRC-URPV2` `URPV2-8a` through `URPV2-8c`; `SRC-PROXY` `PR4b.1`, `PC2.7`, `PM4.5` | Existing: `tests/api/adapters_nonstream/request_controls.rs::chat_request_controls_map_to_messages_upstream`, `tests/api/adapters_nonstream/request_controls.rs::chat_stop_shape_round_trips_same_family`, `tests/api/adapters_nonstream/request_controls.rs::messages_request_controls_map_to_chat_upstream`, `tests/api/adapters_nonstream/request_controls.rs::chat_and_responses_verbosity_map_in_both_directions`, `tests/api/adapters_nonstream/request_controls.rs::messages_metadata_siblings_survive_and_typed_user_wins`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::responses_user_maps_to_messages_while_verbosity_is_omitted`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::messages_user_maps_to_responses_while_stop_is_omitted`. |
| `MSG-NS-13` | An outbound Messages request with active thinking MUST pass the validation rules in `RC4f`. Manual `budget_tokens` MUST be at least 1024 and less than `max_tokens`; generated manual controls obey the same rule. Incompatible sampling controls, forced tool choice, assistant prefill, unsupported manual or adaptive mode, and invalid adaptive effort MUST produce HTTP 400 before upstream dispatch. Monoize MUST NOT mutate user limits or controls to make the request valid. | `SRC-ANTH-MSG-CREATE`; `SRC-ANTH-EXTENDED-THINKING`; `SRC-PROXY` `RC4f` through `RC4f.5` | Existing: `tests/api/adapters_nonstream.rs::messages_thinking_validation_rejects_before_upstream_dispatch`; local: `src/urp/encode/anthropic.rs::thinking_validation_tests::checked_encoder_accepts_valid_manual_and_adaptive_thinking`, `src/urp/encode/anthropic.rs::thinking_validation_tests::checked_encoder_rejects_invalid_manual_budget_relationship`, `src/urp/encode/anthropic.rs::thinking_validation_tests::checked_encoder_rejects_active_thinking_incompatible_controls`, `src/urp/encode/anthropic.rs::thinking_validation_tests::checked_encoder_enforces_adaptive_model_constraints`. |
| `MSG-NS-14` | A same-Messages programmatic tool-calling lifecycle MUST preserve a versioned code-execution descriptor, deferred client-tool `allowed_callers`, top-level `container`, opaque server code-execution blocks, and the `caller` object on a client `tool_use`. | `SRC-ANTH-PTC`; `SRC-PROXY` `TCI8` | Existing API: `tests/api/adapters_nonstream/messages_native.rs::messages_programmatic_tool_calling_round_trips_same_family`. |
| `MSG-NS-15` | A same-Messages tool-search lifecycle MUST preserve versioned regex and BM25 tool-search descriptors, deferred tool metadata, opaque server search blocks, and nested `tool_reference` content without changing block type or order. | `SRC-ANTH-TOOL-SEARCH`; `SRC-PROXY` `TCI9` | Existing API: `tests/api/adapters_nonstream/messages_native.rs::messages_tool_search_lifecycle_round_trips_same_family`. |

### 3.6 Messages stream contract rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `MSG-ST-1` | A downstream `/v1/messages` stream MUST use named SSE events only, MUST start with `message_start`, MUST emit `message_delta` before `message_stop`, and MUST NOT append `[DONE]`. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-ANTH-FULL`; `SRC-PROXY` `FP6a`, `FP6g` | Existing: `tests/api/streaming_messages.rs::messages_streaming_emits_named_sse_events`, `tests/api/streaming_messages.rs::messages_streaming_emits_message_delta_before_stop_for_responses_upstream`. |
| `MSG-ST-2` | Content block lifecycle order is strict. Each block index MUST start once, delta zero or more times, and stop once. Block lifecycles MUST NOT interleave. `content_block_start` indices MUST be contiguous zero-based integers in emission order, with no gaps from skipped or buffered URP nodes. `message_start.message` MUST carry a valid initial envelope. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-ANTH-FULL`; `SRC-PROXY` `ENC9`, `PM9a`, `DM8`, `DM8a`, `FP6g` | Existing: `tests/api/streaming_messages.rs::messages_stream_text_from_responses_upstream_event_sequence`, `tests/api/streaming_messages.rs::messages_stream_text_from_chat_upstream_event_sequence`, `tests/api/streaming_messages.rs::messages_streaming_from_chat_preserves_strict_block_order_in_raw_sse`, `tests/api/streaming_messages.rs::messages_streaming_from_responses_preserves_strict_block_order_in_raw_sse`, `tests/api/streaming_messages.rs::messages_stream_message_start_envelope_fields`, shared `assert_messages_stream_invariants` contiguous-index assertion. |
| `MSG-ST-3` | Thinking block semantics are strict. Thinking deltas MUST preserve upstream chunk granularity, `thinking_delta` MUST precede `signature_delta`, and both surfaces MUST share one thinking block when an ordered Chat detail run contains plaintext immediately followed by an encrypted-only entry. With reasoning envelopes disabled, signature deltas MUST preserve upstream granularity. With reasoning envelopes enabled, raw signature fragments for one thinking block MUST be wrapped once as one complete mz2 envelope; downstream frame splitting MAY divide that envelope, but concatenating the emitted signature strings MUST parse as one envelope whose payload equals the ordered raw-fragment concatenation. Cumulative usage MUST appear on `message_delta`. A same-family Messages decoder MUST merge `message_start.message.usage` with partial or nullable cumulative `message_delta.usage` fields and MUST emit exactly one canonical terminal response after all upstream usage deltas are consumed. Both downstream usage envelopes MUST preserve all typed counters and non-internal unknown fields. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-ANTH-FULL`; `SRC-PROXY` `DM5a.1`, `PM6b`, `PM6c`, `PM12`, `PM12a`, `PR4c.5c`, `DER10` through `DER13`, `FP6g` | Existing: `tests/api/streaming_messages.rs::messages_streaming_preserves_upstream_thinking_delta_granularity`, `tests/api/streaming_messages.rs::messages_streaming_keeps_signature_in_thinking_block_and_delta_order`, `tests/api/streaming_messages.rs::messages_stream_passthrough_preserves_chunked_deltas_and_suppresses_upstream_ping`, `tests/api/streaming_messages.rs::messages_streaming_from_responses_includes_message_delta_usage`, `tests/api/streaming_messages.rs::messages_streaming_merges_partial_usage_and_terminalizes_once`, `tests/api/streaming_messages.rs::messages_streaming_preserves_full_cumulative_usage_at_start_and_terminal`, `tests/api/streaming_messages.rs::messages_stream_signature_delta_does_not_precede_thinking_delta`, `tests/api/streaming_messages.rs::messages_stream_thinking_from_responses_upstream`, `tests/api/streaming_messages.rs::messages_stream_thinking_from_chat_upstream`; local: `src/urp/stream_decode/anthropic.rs::tests::anthropic_partial_usage_is_merged_cumulatively`, `src/urp/stream_decode/anthropic.rs::tests::anthropic_terminal_event_is_taken_once_after_multiple_deltas`. |
| `MSG-ST-4` | Tool use stream reconstruction MUST preserve `tool_use` block starts, streamed `input_json_delta`, and terminal `message_delta.stop_reason = tool_use`, including completed fallback reconstruction. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-URPV2` `TCL-1` through `TCL-2`, `TR-1` through `TR-9`, `STR-7` through `STR-9`; `SRC-PROXY` `ENC9` through `ENC10`, `FP6g` | Existing: `tests/api/streaming_messages.rs::messages_stream_tool_use_from_responses_upstream`, `tests/api/streaming_messages.rs::messages_stream_tool_use_from_responses_completed_fallback`, `tests/api/streaming_messages.rs::messages_stream_parallel_tool_use_from_chat_upstream`, `tests/api/streaming_messages.rs::messages_streaming_maps_tool_use_and_thinking_from_chat_upstream`. |
| `MSG-ST-5` | Same family Messages passthrough streaming MUST preserve the native Anthropic lifecycle instead of reordering or collapsing it. Incremental upstream `thinking_delta`, `text_delta`, and `input_json_delta` frames MUST remain separate downstream frames unless the configured SSE frame limit requires splitting. Signature frames obey the enabled-envelope rule in `MSG-ST-3`. An unknown native block such as `server_tool_use` MUST remain a same-Messages ProviderItem, preserve ordered `input_json_delta` fragments and assembled terminal input, and MUST NOT become client `tool_use`. Terminal `stop_sequence` MUST survive canonical `ResponseDone` and downstream `message_delta`. Upstream `ping` MUST NOT be re-emitted as downstream content and MUST NOT affect content block lifecycle. Downstream keep-alive `ping` frames are generated independently by the downstream response layer after the keep-alive interval. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-PROXY` `FP6a`, `FP6a-CTRL`, `PM6a`, `PM6c`, `PM11a`, `DM7a`, `DM7b`, `DM9a`, `PR4c.5c` | Existing: `tests/api/streaming_messages.rs::messages_stream_passthrough_from_messages_upstream`, `tests/api/streaming_messages.rs::messages_stream_passthrough_preserves_chunked_deltas_and_suppresses_upstream_ping`, `tests/api/streaming_messages.rs::messages_stream_native_server_tool_preserves_deltas_input_and_stop_sequence`; local: `src/urp/stream_decode/anthropic.rs::tests::anthropic_provider_item_input_json_delta_preserves_terminal_input`, `src/urp/stream_decode/anthropic.rs::tests::anthropic_terminal_event_preserves_stop_sequence`. |
| `MSG-ST-6` | Omitted thinking MUST use one valid Messages thinking lifecycle: start with empty `thinking` and empty `signature`, emit zero `thinking_delta` events, emit exactly one non-empty `signature_delta`, then stop the block. A same-family terminal `message_delta` MUST preserve the exact upstream stop reason, including `pause_turn`, `refusal`, and `model_context_window_exceeded`. | `SRC-ANTH-STREAM`; `SRC-ANTH-EVENTS`; `SRC-PROXY` `PM5`, `PM6.2`, `DM5.1` through `DM5b`, `PM11` | Existing: `tests/api/streaming_messages.rs::messages_streaming_preserves_omitted_thinking_lifecycle`, `tests/api/streaming_messages.rs::messages_streaming_preserves_exact_messages_stop_reason`; local: `src/urp/stream_decode/anthropic.rs::tests::anthropic_omitted_thinking_block_completes_with_signature_only`. |
| `MSG-ST-7` | Same-Messages streaming MUST preserve top-level `container`, ordered opaque server code-execution and tool-search blocks, `caller` on streamed client `tool_use`, `code_execution_tool_result`, `tool_search_tool_result`, and nested `tool_reference` content. | `SRC-ANTH-PTC`; `SRC-ANTH-TOOL-SEARCH`; `SRC-PROXY` `TCI8`, `TCI9` | Existing API: `tests/api/streaming_messages.rs::messages_stream_preserves_native_ptc_and_tool_search_lifecycles`. |

### 3.7 Cross protocol tool result, passthrough, and flat URP invariant rows

| Row ID | Behavior under test | Authoritative source | Target coverage |
| --- | --- | --- | --- |
| `XPT-1` | Cross protocol tool result handling MUST keep `ToolResult` as a distinct top level semantic unit. It MUST render as Responses `function_call_output`, chat tool role content, or Anthropic `tool_result` content without reclassifying it as ordinary assistant text. | `SRC-URPV2` `TR-1` through `TR-9`; `SRC-URPTF` `DEC-7` through `DEC-9`, `SURF-9` through `SURF-10`; `SRC-PROXY` `TCI1` through `TCI3`, `ENC1`, `ENC6`, `ENC9`, `ENC10` | Existing: `tests/api/adapters_nonstream.rs::responses_tool_call_flow_nonstream_via_chat_upstream_parallel`, `tests/api/adapters_nonstream.rs::chat_tool_call_flow_nonstream_via_responses_upstream_parallel`, `tests/api/adapters_nonstream.rs::messages_tool_call_flow_nonstream_via_responses_upstream_parallel`, `tests/api/adapters_nonstream.rs::responses_tool_result_multipart_roundtrip_via_responses_upstream`, `tests/api/adapters_nonstream.rs::messages_tool_result_multipart_roundtrip_via_messages_upstream`. |
| `XPT-2` | Top level passthrough is cross family safe, but nested passthrough is not. Top level `extra_body` MUST survive all hops. Ordinary node `extra_body`, `ToolResult.extra_body`, `ToolResultContent.extra_body`, and `next_downstream_envelope_extra` MUST survive same family hops. On a cross family hop, non-internal nested passthrough and control nodes MUST be stripped, while trusted `_monoize_` semantic provenance MUST remain internal until the target adapter consumes or discards it. | `SRC-URPV2` `XTRA-1` through `XTRA-10`, `CTL-4` through `CTL-11`; `SRC-URPTF` `XSTRIP-2` through `XSTRIP-8`; `SRC-PROXY` `F1` | Existing: `tests/api/adapters_nonstream/messages_basic.rs::responses_nonstream_same_family_node_and_envelope_passthrough`, `tests/api/adapters_nonstream.rs::chat_completions_adapter_nonstream`, `tests/api/adapters_nonstream.rs::messages_adapter_nonstream`, `tests/api/adapters_nonstream/messages_basic.rs::cross_family_top_level_extra_survives_while_nested_extra_is_stripped`; local: `src/urp/mod.rs::tests::strip_nested_extra_body_keeps_internal_metadata_and_removes_wire_extras`. |
| `XPT-3` | `next_downstream_envelope_extra` is a one use control node. On same family encode it applies only to the next consumable envelope, never creates an empty envelope, and never crosses a protocol family boundary. | `SRC-URPV2` `CTL-1` through `CTL-11`; `SRC-URPTF` `SURF-4`, `SURF-10`, `XSTRIP-4`; `SRC-PROXY` `DER6` through `DER8` | Existing: `tests/api/adapters_nonstream.rs::responses_same_family_next_downstream_envelope_extra_applies_once`, `tests/api/adapters_nonstream.rs::chat_cross_family_strips_next_downstream_envelope_extra`, `tests/api/adapters_nonstream.rs::messages_cross_family_strips_next_downstream_envelope_extra`. |
| `XPT-4` | `ResponseDone.output` is the authoritative terminal flat stream state. Downstream terminal synthesis, duplicate suppression, completed only reconstruction, and post stream transforms MUST use `ResponseDone.output` instead of helper merged message state. | `SRC-URPV2` `STR-8` through `STR-11`; `SRC-URPTF` `STR-4` through `STR-5`; `SRC-PROXY` `FP6g` | Existing: `tests/api/streaming_responses.rs::responses_streaming_from_responses_upstream_does_not_duplicate_completed_items`, `tests/api/streaming_responses.rs::responses_streaming_response_done_output_is_authoritative_terminal_state`, `src/urp/stream_encode/openai_responses.rs::tests::response_done_output_removes_completed_cache_item_and_emits_terminal_only_lifecycle`, `src/urp/stream_encode/openai_responses.rs::tests::response_done_output_reorders_and_replaces_same_id_items_without_repeating_lifecycles`, `src/urp/stream_encode/openai_chat.rs::tests::response_done_fallback_deduplicates_parallel_tools_by_node_index`, `src/urp/stream_encode/openai_chat.rs::tests::response_done_fallback_emits_later_terminal_only_reasoning_nodes`, `tests/api/streaming_messages.rs::messages_stream_response_done_output_drives_final_message_delta_and_stop`. |
| `XPT-5` | Every non-stream and stream encoder that emits an opaque same-protocol `ProviderItem.body`, and every same-protocol stream encoder that replays `ProviderControl.data`, MUST recursively remove `_monoize_` object keys from a wire-only clone, including keys in objects nested below arrays. It MUST preserve non-internal unknown members and MUST leave the canonical URP body or control data unchanged. Typed and user payloads outside opaque ProviderItem and ProviderControl carriers are outside this sanitizer's scope. | `SRC-URPV2` `XTRA-10`, `STR-13`; `SRC-PROXY` `ENC11` | Existing local: `src/urp/encode/mod.rs::tests::provider_item_wire_body_sanitizer_is_recursive_and_non_mutating`, `src/urp/encode/anthropic.rs::provider_item_tests::messages_provider_block_filters_nested_internal_metadata_on_wire`, `src/urp/encode/openai_responses.rs::tests::responses_provider_item_filters_nested_internal_metadata_on_wire`, `src/urp/encode/openai_chat.rs::tests::chat_provider_part_filters_nested_internal_metadata_on_wire`, `src/urp/encode/gemini.rs::tests::gemini_provider_part_filters_nested_internal_metadata_on_wire`, `src/urp/stream_decode/openai_responses.rs::reserved_wire_extra_tests::unknown_responses_event_remains_canonical_provider_control_data`, `src/urp/stream_encode/anthropic.rs::provider_item_wire_tests::messages_stream_provider_item_filters_nested_internal_metadata`, `src/urp/stream_encode/openai_responses.rs::tests::responses_stream_provider_item_filters_nested_internal_metadata`, `src/urp/stream_encode/openai_responses.rs::tests::responses_provider_control_filters_nested_internal_metadata_without_mutation`, and `src/urp/stream_encode/openai_chat.rs::tests::chat_stream_provider_item_filters_nested_internal_metadata`. |
| `XPT-5a` | Flat URP text phase metadata is a protocol visible invariant only where the downstream family supports it. Same family Responses reconstruction MUST preserve phase, and cross family chat or messages reconstruction MUST strip phase fields that have no native target field instead of inventing a synthetic field. | `SRC-URPV2` `ORD-5`, `ORD-7` through `ORD-8`; `SRC-PROXY` `TPH1` through `TPH5`, `PR4a`; `SRC-URPTF` `XSTRIP-4` | Existing: `tests/api/streaming_responses.rs::responses_streaming_reconstructs_phase_from_output_item_done`, `tests/api/adapters_nonstream.rs::cross_family_phase_is_not_synthesized_for_chat_or_messages`. |
| `XPT-6` | Downstream shape: a `/v1/responses` request that contains a native non-function tool and is routed to a `type=chat_completion` upstream. Upstream target: chat completion family. Expected encoded and decoded tool shape: the native tool is filtered from that outbound attempt because no Chat-valid encoding is specified for that provider-native descriptor. If `tool_choice` selected the filtered descriptor, that `tool_choice` is also omitted for the attempt. Function and custom tools in the same request remain portable. Failure condition: the native tool is forwarded in Responses flat shape to chat, rewritten into a different tool identity, left selectable by `tool_choice` after filtering, or executed locally by Monoize. | `SRC-OAI-RESP-CREATE`; `SRC-OAI-CHAT-CREATE`; `SRC-URPV2` `XTRA-4` through `XTRA-7`; `SRC-PROXY` `T1g`, `PC3`, `TCI2` | Existing: `tests/api/adapters_nonstream.rs::provider_native_tool_filtered_cross_family`. |
| `XPT-7` | Downstream shape: a tool definition whose semantic fields conflict with same-named entries in tool-local extra data, for example typed `name`, `parameters`, `input_schema`, `strict`, or `cache_control` plus an extra value under the same key. Upstream target: each same-family encoder for Responses, Chat, and Messages. Expected encoded and decoded tool shape: the semantic field value wins and the conflicting extra value is not emitted to the same key. Failure condition: the extra value overwrites the semantic field, both values are emitted under the same key, or the conflict escapes through top level request `extra_body`. | `SRC-URPV2` `URPV2-7`, `ORD-8`, `XTRA-1` through `XTRA-3`; `SRC-PROXY` `T0`, `T1`, `PR4`, `PC3`, `PM1`, `XF6d` | Existing local: `src/urp/encode/openai_responses/media.inc.rs::tests::tool_definition_extra_collision_semantic_fields_win`, `src/urp/encode/openai_chat.rs::tests::chat_tool_definition_conflicts_prefer_semantic_fields`, `src/urp/encode/anthropic/tools.inc.rs::tests::anthropic_tool_extra_layering_is_stable`. |
| `XPT-8` | A freeform custom tool definition is portable only between Chat and Responses. Chat-to-Responses encoding MUST use a flat Responses custom descriptor; Responses-to-Chat encoding MUST use the nested Chat custom descriptor. When the selected target is Messages, Monoize MUST omit the freeform custom descriptor and any `tool_choice` that selected it because Messages has no equivalent freeform custom-tool surface. Monoize MUST NOT synthesize an Anthropic user-defined JSON-schema tool from that descriptor. | `SRC-OAI-RESP-CREATE`; `SRC-OAI-CHAT-CREATE`; `SRC-ANTH-MSG-CREATE`; `SRC-URPV2` `XTRA-4` through `XTRA-7`; `SRC-PROXY` `T1`, `T1a`, `PR4`, `PM1` | Existing: `tests/api/adapters_nonstream/controls_tools_matrix.rs::openai_custom_tool_definitions_use_target_family_shape`; local: `src/urp/encode/openai_chat.rs::tests::openai_chat_custom_tool_round_trips`. |
| `XPT-9` | Unknown members nested inside Chat or Responses usage detail objects MUST remain nested under the same detail-object key across decode and encode. Typed counters MUST overwrite colliding preserved values. Non-stream and stream terminal usage encoding MUST use the same shape. | `SRC-OAI-RESP-CREATE`; `SRC-OAI-CHAT-CREATE`; `SRC-PROXY` `XF5b` through `XF5c` | Existing local: `src/urp/decode/openai_chat.rs::tests::chat_usage_preserves_nested_unknown_details`, `src/urp/decode/openai_responses.rs::tests::responses_usage_preserves_nested_unknown_details`, `src/urp/stream_encode/openai_chat.rs::tests::chat_stream_terminal_usage_preserves_nested_unknown_details_and_typed_counters_win`, `src/urp/stream_encode/openai_responses/encode_loop_part2.inc.rs::tests::responses_stream_terminal_usage_preserves_nested_unknown_details_and_typed_counters_win`; API: `tests/api/adapters_nonstream.rs::chat_nonstream_usage_preserves_nested_unknown_details`, `tests/api/adapters_nonstream.rs::responses_nonstream_usage_preserves_nested_unknown_details`. |
| `XPT-10` | Chat and Responses freeform custom-tool calls and results MUST round-trip in-family and convert bidirectionally across those two OpenAI protocol families. The canonical call/result discriminator MUST select `custom` rather than `function`. Responses streaming MUST preserve custom input delta/done event names. Messages targets MUST omit custom calls and results because no freeform custom-call lifecycle is specified for Messages. | `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-PROXY` `ENC3`, `PR8a`, `PC6`, `PC7` | Existing: `src/urp/encode/openai_chat.rs::tests::custom_tool_call_lifecycle_round_trips_and_cross_maps_chat_responses`, `src/urp/stream_decode/openai_responses/decode_helpers.inc.rs::tests::custom_tool_call_input_delta_and_done_decode`, `src/urp/stream_encode/openai_responses/encode_loop_part2.inc.rs::tests::custom_tool_call_input_delta_and_done_encode`. |
| `XPT-11` | Function, custom, and `allowed_tools` selectors MUST use the target protocol's official shape. URP MUST normalize Responses flat function and custom selectors plus flat `allowed_tools` into canonical nested selectors, then emit flat selectors to Responses and nested selectors to Chat. Filtering MUST remove unavailable allowed references, remove an empty `allowed_tools` choice, match MCP server descriptors by `server_label`, and recursively reject client-supplied `_monoize_` selector keys. This rejection applies to modern Chat selectors, deprecated Chat `function_call` selector provenance, Messages selectors, and non-object `ToolChoice::Specific` fallback values at every target encoder. | `SRC-OAI-CHAT-CREATE`; `SRC-OAI-RESP-CREATE`; `SRC-URPV2` `XTRA-10`; `SRC-PROXY` `T1f.1` through `T1f.3` | Existing API: `tests/api/adapters_nonstream/controls_tools_matrix.rs::responses_tool_choice_variants_use_flat_responses_shapes`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::allowed_tools_uses_each_openai_target_family_wrapper`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::allowed_tools_without_definitions_is_omitted`, `tests/api/adapters_nonstream/controls_tools_matrix.rs::allowed_tools_prunes_only_unavailable_references`; local: `src/urp/encode/mod.rs::tests::target_specific_tool_choice_shapes_preserve_semantics`, `src/urp/encode/mod.rs::tests::target_tool_choice_fallbacks_reject_recursive_internal_keys`, `src/urp/decode/openai_responses.rs::tests::official_responses_tool_choice_variants_normalize_and_round_trip`, `src/urp/decode/openai_responses.rs::tests::responses_tool_choice_rejects_recursive_internal_key_spoofing`, `src/urp/decode/openai_chat.rs::tests::chat_tool_choice_rejects_recursive_internal_key_spoofing`, `src/urp/decode/openai_chat.rs::tests::legacy_chat_function_choice_provenance_rejects_recursive_internal_key_spoofing`, `src/urp/decode/anthropic.rs::tests::messages_tool_choice_rejects_recursive_internal_key_spoofing`. |
| `XPT-12` | A decoder MUST reject an incoming `_monoize_` member at every wire-owned passthrough boundary while preserving non-internal vendor fields. This rule applies to non-stream usage objects and nested usage-detail extras, Responses event extras, Messages block and usage extras, Image event extras, and Chat choice, delta, terminal-message, and modern tool-call extras. A client-supplied modern Chat tool call MUST NOT activate the deprecated-function marker. Decoder-created internal markers MUST remain available until their target encoder consumes them. The rejection rule MUST NOT recursively alter arbitrary typed or user payloads. | `SRC-URPV2` `XTRA-10`; `SRC-PROXY` `T1f.3` | Existing local: `src/urp/decode/mod.rs::tests::split_extra_rejects_wire_spoofing_of_internal_metadata`, `src/urp/decode/openai_chat.rs::tests::chat_usage_preserves_nested_unknown_details`, `src/urp/decode/openai_responses.rs::tests::responses_usage_preserves_nested_unknown_details`, `src/urp/decode/gemini.rs::tests::gemini_usage_rejects_reserved_wire_keys_and_preserves_vendor_extras`, `src/urp/stream_decode/openai_responses.rs::tests::responses_stream_split_rejects_reserved_wire_keys_and_preserves_vendor_extras`, `src/urp/stream_decode/anthropic.rs::tests::messages_stream_rejects_reserved_wire_extras_but_keeps_decoder_markers`, `src/urp/stream_decode/openai_image.rs::tests::completed_payload_decodes_base64_image_node`, `src/urp/stream_decode/openai_chat.rs::tests::chat_stream_rejects_reserved_wire_extras_and_modern_legacy_marker_spoof`. |

## 4. Execution rule for later tasks

PTM-7. A later flat URP v2 task that changes one matrix row's behavior is not complete until the row's target coverage is implemented or updated and the resulting tests validate the cited authority.

PTM-8. If a later task discovers that one row cites the wrong authority or wrong target location, that task MUST update this matrix in the same change that updates the tests.
